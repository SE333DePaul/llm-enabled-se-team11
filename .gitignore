# ── Build output ──────────────────────────────────────────────
target/

# ── IDE files ─────────────────────────────────────────────────
.idea/
*.iml
.vscode/
*.classpath
*.project
.settings/

# ── API keys — NEVER commit your real key ─────────────────────
# If you put your key directly in application.properties,
# add that file here. Use a separate application-local.properties
# for local overrides so the template stays safe.
application-local.properties

# ── H2 database files ────────────────────────────────────────
data/

# ── macOS / Windows clutter ───────────────────────────────────
.DS_Store
Thumbs.db

# ── Local planning notes (keep off the remote) ────────────────
final-plan.md
